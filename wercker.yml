box: wercker/golang

build:
  steps:
    - script:
        name: versions
        code: |
          git version
          go version
    - script:
        name: run tests
        code: |
          go test
