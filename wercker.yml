box: tcnksm/gox
build:
  steps:
    - setup-go-workspace
    - script:
        name: go get
        code: |
          go get -t ./...
    - script:
        name: go test
        code: |
          go test -v ./...
deploy:
  steps:
    - setup-go-workspace
    - script:
        name: install zip
        code: |
          apt-get update
          apt-get install -y zip
    - motemen/gobump-github-pull-request:
        github_token: $GITHUB_TOKEN
        label_pattern_minor: ^feature$
    - script:
        name: tag version
        code: |
          if [[ "$(git log -1 --pretty='%an %s')" == werckerbot\ bump\ * ]]; then
            export RELEASE_TAG="v$(gobump show | jq -r '.[]')"
          fi
    - tcnksm/gox:
        os: darwin linux windows
    - tcnksm/zip:
        input: $WERCKER_OUTPUT_DIR/pkg
        output: $WERCKER_OUTPUT_DIR/dist
    - tcnksm/ghr:
        token: $GITHUB_TOKEN
        input: $WERCKER_OUTPUT_DIR/dist
        replace: true
        version: $RELEASE_TAG
